# Dependencies
node_modules/

# Environment variables (never commit real secrets)
.env
.env.local
.env.*.local

# LowDB data file (contains real student PII — must never be committed)
server/veritas.json
server/*.db

# Uploaded PDF attachments (real student files — must never be committed)
server/uploads/

# Build output
client/dist/
client/.vite/

# Logs
npm-debug.log*
*.log

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Local duplicate repos / accidental files
veritas-prep-final/
veritas-prep-v3/
server/{console.error(e)
